# 프로젝트 개발 지침

## 목표

이 저장소는 단일 첼로 학원 랜딩페이지에서 시작해 회사, 레스토랑, 웨딩 사이트를 같은 CMS에서 제작·운영할 수 있는 다중 카테고리 랜딩페이지 플랫폼으로 확장한다.

## 필수 원칙

- 기존 첼로 랜딩페이지의 02B 디자인과 동작을 회귀시키지 않는다.
- 업종 차이는 조건문 묶음이 아니라 `category`, `template`, `section schema`로 표현한다.
- 고객 프로젝트의 콘텐츠와 미디어는 `projectId` 기준으로 격리한다.
- 권한은 소유자, 편집자, 검토자로 유지하며 서버 단계에서는 RLS로 다시 검증한다.
- 편집 화면과 실제 랜딩페이지는 동일한 렌더링 컴포넌트를 사용한다.
- 이미지 캐러셀은 최소 3장을 유지한다.
- PC와 모바일 이미지 초점은 각각 설정하되 X와 Y를 10~90% 범위로 제한한다.
- 원본 이미지를 보존하고 표시용 파생 이미지를 별도로 관리한다.
- 비밀키는 클라이언트 번들이나 Git 저장소에 포함하지 않는다.
- 공개 전 데이터 검증, 변경 비교, 버전 스냅샷, 롤백 경로를 유지한다.
- DB 스키마와 RLS 변경은 `db/migrations/`의 SQL migration으로 관리하며 운영 DB에서만 수정하지 않는다.
- 백엔드 파일은 `server/README.md`의 책임 경계를 따르며 인증은 `server/auth/`, HTTP 계약은 `server/routes/`, 관리자 실행 명령은 `server/scripts/`에 둔다.
- 공개본은 불변 Release로 저장하고 소유자 전용 공개·롤백 경로를 사용한다.
- 브라우저는 PostgreSQL에 직접 연결하지 않고 자체 Fastify API만 사용한다.
- 인증 암호학은 직접 구현하지 않고 저장소에 포함된 Better Auth를 사용하되 사용자·권한·CMS 규칙은 API와 PostgreSQL이 소유한다.
- API 사용자 쿼리는 트랜잭션마다 `app_runtime` 역할과 `app.user_id`를 로컬 범위로 설정해 RLS를 적용한다.
- migration·함수 소유자와 runtime DB 역할을 분리하고 runtime 역할에는 `BYPASSRLS`나 테이블 소유권을 주지 않는다.
- API runtime 환경에는 `DATABASE_MIGRATION_URL`을 주입하지 않고 runtime·관리자 환경파일과 배포 비밀을 분리한다.
- 인증 요청 URL은 설정된 `BETTER_AUTH_URL`을 기준으로 만들며 외부 `Host`·forwarded 헤더를 기준 URL로 신뢰하지 않는다.
- API 프로세스는 시작 실패와 `SIGINT`·`SIGTERM`에서 Fastify와 모든 DB pool을 중복 없이 종료한다.
- DB 백업이 미디어 객체를 포함한다고 가정하지 않고 PostgreSQL과 객체 스토리지를 별도로 백업·복원 검증한다.

## 문서 갱신 규칙

기능 또는 범위가 변경되면 `README.md`의 다음 항목을 함께 갱신한다.

1. 마지막 업데이트
2. 전체 진행률
3. 구현된 기능
4. 진행 중인 MVP 체크박스
5. 최근 검증 결과
6. 다음 작업과 차단 요소

완료 표시는 코드 작성만으로 판단하지 않는다. 빌드, 브라우저, 데이터 또는 통합 검증 중 해당 기능에 필요한 검증이 끝나야 `[x]`와 완료 상태를 사용할 수 있다.

## 검증

- 모든 변경 후 `npm run build`를 실행한다.
- UI 또는 권한 변경은 데스크톱 브라우저에서 실제 동작을 확인한다.
- 반응형 변경은 최소 데스크톱 1440px, 태블릿 768px, 모바일 390px을 확인한다.
- PostgreSQL/API/Cloudflare/R2 연결은 로컬 빌드 성공만으로 완료 처리하지 않고 해당 환경의 실제 통합 테스트 결과를 기록한다.
